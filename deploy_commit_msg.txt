修正補課規定設定頁儲存按鈕浮動異常留白

- 問題：儲存按鈕改 sticky 浮動後，畫面底部按鈕上方留了一截空白、看得到下方內容
  卡片穿透，原因是 sticky 元素用了負值 margin（bleed 到容器邊緣）跟 sticky 的
  定位計算互相干擾
- ui/admin/panel_makeup_rules.js：拿掉負值 margin，改用容器原本的內距，不再
  bleed 到邊緣（背景色跟頁面背景色一致，視覺上不會有色差，只是左右會有跟其他
  卡片一致的內距，不會頂到邊）
