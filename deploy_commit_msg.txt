義工櫃台：編輯補課排除逾期/占用堂次、確認框改善、取消登記後清單連動刷新（重構52/52b/52c）

- kiosk_edit_makeup（後端已補逾期檢查，隨 SQL 部署，不在本次前端範圍）
- toggleEditMakeupForm：編輯補課登記的「缺課堂次」下拉排除已逾期、已被其他登記占用的堂次
  （正在編輯的這一堂本身仍保留可選）
- renderMakeupRegisterForm：新增補課登記時，已登記過的缺課 checkbox 改為 disabled，
  避免不小心勾選、被 register_makeup 的 ON CONFLICT 靜默覆蓋原本登記內容
- 新增 kioskInlineConfirm（畫面內確認框，取代瀏覽器原生 confirm()）：起因是義工取消登記時
  confirm() 彈窗容易按太快漏按確定，以為取消成功、資料其實沒刪掉。套用到「今日補課」清單、
  「到場提醒/未到場」清單、「今日登記清單」三處的取消登記/取消到場按鈕
- loadAlertsAndRegistrations 的 onCancelMakeup／onCancelTransfer：改呼叫 loadDay() 取代單純呼叫
  自己，讓「今日登記清單」取消後「今日補課」清單也會跟著連動刷新，不用手動整頁重新整理
- 純前端，本次無新增 SQL 異動（kiosk_edit_makeup 的逾期檢查等 SQL 已於稍早單獨部署到 Supabase）
